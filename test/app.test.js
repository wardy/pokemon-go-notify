var assert = require('assert');

describe('Pokemon Notify', function() {
  describe('NotificationTracker', function() {
    it('should clear pokemon that have expired', function() {

    });
    it('should return add a pokemon to the notifiedMons array if that pokemon has not been notified before', function() {

    });
  });
});